# Reader Code Rules

- 阅读器相关代码位于 `ruoyi-modules/ruoyi-reader/**`。
- 阅读器相关数据库脚本位于 `script/sql/ry_reader.sql`、`script/sql/upgrade/**` 与阅读器相关初始化/升级脚本。
- 从 2026-08-14 起，所有阅读器需求的新增或修改代码都必须同步补齐注释。
- 从 2026-08-15 起，所有阅读器需求的新增或修改 SQL 结构脚本也必须同步补齐表注释、字段注释和必要的脚本说明。
- 类/文件注释、字段注释、公开方法注释、私有方法注释、关键流程块注释都属于必做项，不能只补类注释。
- `private final` 依赖注入成员、`private static final` 常量、关键配置项也要补注释，说明它们在当前类中的业务职责。
- `Service`、`Controller`、`Job`、`Cache`、`Parser`、`Mapper`、`Utils`、`Vue` 页面、`TypeScript` 类型文件都要按对应粒度补注释。
- 方法体内凡是状态流转、条件分支、批量查询映射、缓存读写、容错回退、跨端差异处理等关键逻辑，都必须补流程注释说明“为什么这样做”。
- 当文件里存在多组不直观的 import 或跨模块依赖时，要补依赖分组说明注释，解释请求工具、共享类型、平台能力或第三方能力的引入原因。
- JSON 等不支持注释的配置文件，不要写非法注释，改把说明写到相邻代码或文档里。
- 每张阅读器业务表都必须有表注释，明确该表在导入、审核、发布、阅读、书架、进度等链路中的职责。
- 每个阅读器业务字段都必须有字段注释，至少说明字段含义、单位、状态枚举、引用关系或使用场景。
- 新增字段、修改字段、状态扩展、定位值字段、时间字段、开关字段时，不能只改结构不改注释。
- 升级脚本中的 `ALTER TABLE`、`ADD COLUMN`、`MODIFY COLUMN` 也必须同步维护注释，保证新库初始化脚本与旧库升级后的元数据一致。
- 如果已将结构脚本执行到真实数据库，还需要确认远端库表注释和字段注释已经实际生效，不能只停留在仓库文件层面。
