# 书源批量采集与任务维护进度

## 2026-09-13 当前运行诊断与修复

- [x] 临时将 PC 客户端 `e5cd7e4891bf95d1d19206ce24a7b32e` 的 `active_timeout`、`timeout` 设置为 `-1`；该配置作用于所有 PC 客户端登录，恢复时使用 `1800` 秒、`604800` 秒。修改后需重新登录，旧令牌不会自动延长。
- [x] 任务列表和配置详情增加最新运行记录展示字段，区分 `WAITING_WORKER`（等待 Worker 领取）与 `COLLECTING`（Worker 已领取并执行），避免把数据库中已创建但尚未领取的运行误显示成实际采集中。
- [x] 后端调度对站点熔断期间尚未领取的运行记录改为 `PAUSED/CIRCUIT_OPEN`，保留运行、错误和自动化日志，等待管理员执行“重置熔断重试”。
- [x] 修复 bookcheng8 目录选择器：原 `#list dl dd` 会把站外推荐书链接当成章节，导致 `source URL leaves the approved host` 并累计熔断；现改为 `#list dl dd a[href^='/']`，并在 Python Worker 增加同站过滤。实测《血衣梦》目录由 98 项误识别降为 88 个站内章节。
- [x] 重建后端和管理端，重启 `codex.book.backend` 与 `codex.book.source-worker`；后端监听 `8080`，Worker 使用 `python-worker-hot-ranking-01` 正常轮询。
- [x] 当前诊断：正式任务 19 条运行中记录中，只有已领取记录才代表实际执行；大量未领取记录受 Worker 总并发和每站点并发限制排队。三个正式站点均为每分钟 2 次、最小间隔 15 秒、每日 10000 次，速度受授权策略约束，不能通过提高并发绕过。
- [x] 当前失败主因：bookcheng8 的 6 条历史失败任务包含 403/520 以外的解析与站外链接错误，修复前规则导致 `source URL leaves the approved host`；dushuwo/bqg5555 仍有超时、解析失败和排队任务，需按错误分类有界重试。

- [x] Slice 1：数据模型和契约（已完成；后端编译、前端构建通过）
- [x] Slice 2：服务端去重与任务书籍接口（去重、增量、分类自动登记和终态流转已完成）
- [x] Slice 3：Worker 批量采集协议（Java、Python、Go 均已接入发现、逐书领取和终态回执）
- [x] Slice 4：管理端任务维护（模式表单、任务书籍明细、正文快照和 5 秒进度刷新已完成）
- [x] Slice 5：验证与文档（2026-09-05 已完成 bookcheng8 授权站点真实采集验收）

## 2026-09-05 真实验收记录

- 站点：`www.bookcheng8.com`，授权状态 `APPROVED`，规则版本 `1`。
- 单本任务 `2096317600740143106`：`SINGLE`，`book_limit=1`，完成并进入 `WAITING_REVIEW`，写入 8 个章节快照。
- 全站任务 `2096317600849195010`：`ALL`，`book_limit=3`，发现并完成 3 本，3/3 本、6 个章节快照、进度 100%。
- 玄幻任务 `2096317600924692481`：`CATEGORY`，`book_limit=2`，完成 2 本、4 个章节快照、进度 100%。
- 言情任务 `2096317600995995649`：`CATEGORY`，`book_limit=2`，完成 2 本、4 个章节快照、进度 100%。
- 仙侠任务 `2096317601063104513`：`CATEGORY`，`book_limit=2`，完成 2 本、4 个章节快照、进度 100%。
- 所有真实批量快照正文均通过中文内容校验；章节范围按本次任务配置采集第 1-2 章。
- 已核验任务书籍明细、运行记录、章节快照、`reader_source_task.book_limit` 数据结构和三类来源分类。
- 当日实现仅把任务状态置为 `WAITING_REVIEW`，未创建作品、章节及审核记录；该缺口已在 2026-09-06 的入库物化修复中补齐。
- bookcheng8 测试策略已恢复原值：`requests_per_minute=2`、`daily_request_limit=10`、`min_delay_ms=15000`。

## 2026-09-06 入库物化与管理端补齐

- [x] 修复采集入库链路：Worker 写入章节快照后，自动创建或复用草稿作品，同步创建章节、章节正文和 `PENDING` 内容审核记录，并回填任务书籍与快照的 `work_id`。
- [x] 增加历史任务补偿接口 `POST /reader/admin/source/tasks/{taskId}/materialize`，已对 5 个真实验收任务完成补偿入库。
- [x] 内容审核增加 `source_task_id`、`source_task_book_id` 关联；审核通过后会确认快照、完成任务书籍，并在无待处理快照时完成采集任务。
- [x] 增加作品分类后端接口、管理端页面、路由、权限与菜单；分类列表已真实返回玄幻魔法、言情小说、仙侠修真、都市小说 4 条来源分类。
- [x] 采集任务增加“配置详情”抽屉，可查看站点、规则、策略、采集模式、分类、`book_limit`、章节范围、增量设置、来源地址、进度和失败原因。
- [x] 数据库升级脚本 `script/sql/upgrade/2026-09-06-reader-source-ingestion.sql` 已执行；审核关联字段和书源采集中心、作品分类菜单均已验证存在，菜单已授权管理角色。
- [x] 真实数据核验：作品管理 10 本草稿作品，内容审核 10 条 `PENDING` 记录，章节 20 条，非空章节正文 20 条；9 条批量任务书籍和本轮 26 条章节快照均已关联作品。
- [x] API 核验：作品列表、待审核列表、作品分类、作品目录、章节正文和 5 个任务配置详情均返回真实数据；《诡秘之主》章节预览已返回完整中文正文。
- [x] 回归验证：`ReaderSourceWorkerServiceTest`、`ReaderAuditServiceTest`、后端 Maven 打包、前端 Vite 构建、前端改动范围 oxlint 和两仓库 `git diff --check` 均通过。
- [ ] 项目全量 `vue-tsc --noEmit` 仍受既有问题阻断：监控模块 `loginInfo/logininfo` 文件名大小写冲突，以及书源中心 Element Plus 表格插槽的 `DefaultRow` 类型推断错误；不影响本次 Vite 构建产物生成。

## 2026-09-06 H5 发布展示核验

- [x] 定位 H5 未展示采集作品的根因：10 本作品和 20 个章节均为 `DRAFT`，10 条审核记录为 `PENDING`，C 端接口按设计只返回 `PUBLISHED` 内容。
- [x] 通过正式内容审核接口批准 10 条采集审核记录；作品与章节全部转为 `PUBLISHED`，26 条快照转为 `CONFIRMED`，5 个采集任务和 9 条任务书籍明细全部转为 `COMPLETED`。
- [x] 核验 H5 实际接口：首页栏目已返回采集作品，书城排行榜返回 10 本作品；《诡秘之主》详情、2 章目录和第一章 1387 字正文均可正常读取。
- [x] 修正 PC 管理端状态操作：审核页仅对未通过记录显示“通过”，已通过记录明确显示结果；作品页按发布状态只显示当前可执行的“上架”或“下架”，未审核作品显示“审核后上架”，不再用一组禁用按钮误导操作。

## 2026-09-06 小说封面补齐

- [x] 新增缺图自动封面服务：作品没有来源封面时生成 `600x800` PNG，随机使用不同配色和纹理，并使用系统中文字体自动缩放、换行绘制完整小说名和作者。
- [x] 采集建档和管理端手工建档均已接入自动封面；作品已有外部来源封面时保持原地址，不覆盖来源图片。
- [x] 增加公开封面接口 `GET /reader/app/covers/{workId}.png` 和管理端补偿接口 `POST /reader/admin/works/covers/backfill`，图片接口允许浏览器 `<img>` 无令牌加载。
- [x] 已真实执行历史回填，接口返回 `10`；数据库 10/10 本作品均有唯一 `cover_url`，运行目录存在 10 张不同的 PNG。
- [x] 已逐张核验公开接口：10/10 返回 `200 image/png`，图片尺寸全部为 `600x800`；长标题《把法院告上法庭，你说这是律师？》完整显示且未越界。
- [x] H5 已将 `/reader/...` 相对封面地址解析到后端 API；浏览器实测书城 3 张首屏封面均从 `localhost:8080` 成功加载，天然尺寸为 `600x800`，不再显示默认占位图。
- [x] 回归验证：后端封面/采集/作品/控制器 16 项测试通过，H5 11 项测试通过，后端完整 Maven 打包和 H5 Vite 构建通过；后端已恢复默认安全配置运行在 `8080`，H5 运行在 `4173`。

## 2026-09-06 浅色横竖双封面升级

- [x] `reader_work` 新增 `cover_landscape_url`，竖版继续使用 `cover_url`；升级脚本 `script/sql/upgrade/2026-09-06-reader-work-landscape-cover.sql` 已执行并通过 `SHOW COLUMNS` 验证。
- [x] 自动封面改为浅色配色，单本同时生成 `600x800 portrait.png` 和 `1200x675 landscape.png`，两种图片均包含完整作品名和作者；旧 `/{workId}.png` 读取地址继续兼容。
- [x] 自动封面 URL 增加 `v=light-dual-1` 设计版本，确保已打开过 H5 的浏览器不会继续使用旧深色封面缓存。
- [x] 后端实体、管理端 VO、App 卡片/详情/书架接口均已返回 `coverLandscapeUrl`；已有外部横竖封面保持原地址，缺少的方向才自动生成。
- [x] 已通过管理端正式回填接口重新处理现有 10 本作品；数据库竖版 10/10、横版 10/10 且地址均唯一，20 个新公开图片接口全部返回 `200 image/png`。
- [x] H5 首页横幅和宽卡使用横版封面，榜单、书架、详情等竖卡使用竖版封面；390x844 移动端实测无横向溢出、图片无空白，控制台无错误。
- [x] 验证结果：阅读器模块 116 项测试通过，H5 11 项测试通过，后端全量 Maven 打包和 H5 Vite 构建通过；后端运行在 `8080`，H5 运行在 `4173`。

## 2026-09-06 全局封面背景与正文排版

- [x] 新增全局封面配置 `reader.cover.defaultStyle`，支持纯色和 OSS 背景图片；内置樱粉、薄荷、晴空、杏黄、藕荷、珍珠灰 6 种浅色预设。
- [x] 作品支持 `GLOBAL`、`COLOR`、`IMAGE` 三种背景模式；作品管理可维护全局背景，也可逐本继承全局、选择预设/自定义颜色或上传背景图。
- [x] 采集建档和自动封面生成统一读取全局或作品级配置，并同时生成 `600x800` 竖版和 `1200x675` 横版封面；历史 10 本作品已重新生成，封面版本均为 `2`。
- [x] 数据库升级脚本 `script/sql/upgrade/2026-09-06-reader-cover-style-and-novel-format.sql` 已执行并验证；`reader_work` 4 个背景字段和全局配置均存在，当前全局背景为 `#FFF4F2`。
- [x] Java/Python/Go Worker、后端入库和 H5 展示统一保留自然段，兼容换行、连续普通空格、NBSP 和连续全角空格；H5 使用独立段落、左对齐、首行缩进和固定段间距。
- [x] 历史正文已实际迁移：20/20 章节和 33/33 快照均含标准双换行，连续普通/全角空格残留均为 0，33/33 快照 SHA-256 哈希与格式化正文一致。
- [x] 验证结果：后端格式化 3 项、Python 2 项、Go、Java Worker 编译和 H5 14 项测试通过；后端 41 模块、管理端与 H5 生产构建通过。
- [x] 服务已重启并验收：后端 `8080`、管理端 `80`、H5 `4173`；H5 在 `390x844` 下正文左对齐、首行缩进 36px、段距 18px，无横向溢出。
- [x] H5 阅读页验收发现历史升级脚本 `script/sql/upgrade/2026-08-29-reader-bookmark.sql` 漏执行；已连续执行两次验证幂等性，书签表、唯一键和索引均已核验，真实 H5 会话重新加载后控制台错误为 0。

## 2026-09-07 三书源分类热门前十全章节

- [x] 授权核验：`www.bookcheng8.com`、`m.dushuwo.net`、`www.bqg5555.cc` 当前均为启用的 `APPROVED`，且授权备注为“已授权，可以采集”。
- [x] 数据库配置：三站各 8 个分类任务，共 24 个任务、计划发现 240 本；均为 `CATEGORY`、`book_limit=10`、从第 1 章采集到末章、`PYTHON` 执行器。任务 ID：bookcheng8 `9000000000000000013-0020`、dushuwo `9000000000000000028-0035`、bqg5555 `9000000000000000043-0050`。
- [x] 新增并执行 `2026-09-07-authorized-source-hot-ranking-more-sites.sql` 两次，已验证幂等；`dushuwo` 与 `bqg5555` 各自使用单站点并发 1、每分钟 2 次、15-30 秒间隔的低频策略。
- [x] 三种 Worker 支持章节目录按标题章节号排序、作品 URL 去重，以及声明式 `chapter.nextPage` 拆页正文合并；`bqg5555` 单章多页正文不会只保留第一页。
- [x] 连载状态：三种 Worker 统一识别 `完结/全本/大结局` 为 `FINISHED`、`连载` 为 `ONGOING`；数据库 `reader_source_task_book.serial_status` 已验证存在，H5 作品卡片和详情页均已接入展示。
- [x] 动态 Skill：`.agents/skills/authorized-source-collector` 支持动态书源、分类、榜单、TopN、章节范围、执行器与授权条件，并补充拆页章节规则。
- [x] H5 卡片公共 VO 已补齐 `serialStatus`，首页、书城/搜索、相关推荐和榜单映射均返回该字段；Reader 模块 124 项测试通过，后端已重新打包。公开书城接口实测返回 `serialStatus=ONGOING`，H5 17 项测试和生产构建通过，浏览器详情页实测显示“状态：连载中”。
- [x] 后端已使用最新 JAR 重新加载为 LaunchAgent `codex.book.backend`，稳定监听 `8080`，当前 PID `59758`；Python Worker 已使用项目 `.venv` 和相同密钥加载为 LaunchAgent `codex.book.source-worker`，当前 PID `61144`，已连续通过 Worker 认证并轮询认领接口，日志位于 `logs/reader-source-python.log`。
- [x] 已通过正常登录的管理端正式启动 24 个任务，当前均为 `RUNNING`；未绕过验证码，也未伪造运行或完成记录。
- [ ] 持续核验任务书籍、章节快照、作品、审核、章节正文、分类和连载状态。三个站点均采用低频策略，全量 240 本及全部章节属于长时间后台任务，不应提前标记完成。

## 2026-09-07 开放章节范围修复

- [x] 根因确认：批量任务 `end_chapter_no` 为 `NULL` 时，Java 后端错误地把发现阶段的 `remote_latest_chapter_no` 摘要当作采集终点，导致部分真实采集只处理到第 2 章。
- [x] Java 后端已修复领取和结果校验逻辑：开放范围始终保持 `endChapterNo = null`，仅在任务明确配置结束章节时限制章节号；发现摘要只作为展示元数据。
- [x] Python、Go、Java Worker 的开放范围测试均已补充，Java `ReaderSourceWorkerServiceTest`、Python Worker 测试和 Go Worker 测试均通过。
- [x] 后端已重新打包并重启，当前监听 `8080`；Python Worker 已重新加载修复版本并持续运行。
- [x] 真实验证任务书籍 `2096969922507853825` 已写入 40 个非空章节快照，章节号范围为 1-44，已确认不再固定为 2 章；对应草稿作品、40 个章节正文和待审核记录已生成。
- [x] 继续核查发现 bqg5555 目录每隔若干章使用无 `href` 的脚本链接，旧目录解析会漏掉第 5、15、25、35 等章节；三种 Worker 已支持从每章末页公开的同站“下一章”链接动态补齐目录空档，内部链接字段不会提交后端。
- [x] bqg5555 规则已增加 `chapter.nextChapter=#next_url` 和 `chapter.nextChapterText=下一章`；Python 10 项、Go 全部测试及 Java Worker 打包通过，Python Worker 已重新加载新代码。
- [ ] 该任务仍在按授权站点低频策略运行，尚未到来源目录末尾；24 个三书源任务均已启动并需持续核验，不能提前标记完成。

## 2026-09-08 任务卡住诊断与恢复

- [x] 真实状态核验：三站 24 个任务均为 `book_limit=10`、开放章节范围（`end_chapter_no=NULL`）；截至本次检查尚未全部采集完成。
- [x] bqg5555 实际完成 2 本书：任务 `9000000000000000046` 完成《从军赋》220 个非空章节，任务 `9000000000000000048` 完成《无限恐怖之最强中州队》100 个非空章节；两任务均为 1/10 本、9 本排队，未完成任务。
- [x] bqg5555 任务 `9000000000000000043-0050` 已统一暂停；Redis 当日计数为 `1000/1000`，原因是站点每日请求额度耗尽，不是章节已经采完。任务 47、49、50 的卡住书籍已恢复为 `QUEUED`。
- [x] dushuwo 已真实发现榜单书籍，任务 33-35 分别已有 10 本明细；当前按每分钟 2 次、15-30 秒间隔逐本处理，尚未产生章节快照。
- [x] bookcheng8 任务尚未发现书籍；其站点策略仍为每分钟 2 次、每日 10 次、15-30 秒间隔，不能通过提高频率绕过授权策略。
- [x] 修复运行状态误导：旧 Worker 因每日额度耗尽无限等待，已停止并重启；后端已加载最新 JAR，Worker 已加载最新 Python 代码。每日额度耗尽现在会回传错误并将任务置为 `PAUSED`，不再无限轮询。
- [x] 修正批量任务状态：已完成章节的任务不会在仍有排队书籍时进入 `COMPLETED`；46、48 的任务进度已回填为 1/10（10%）。
- [x] 全库内容核验：当前作品 11 本、章节 338 条、非空正文 338 条，均为已发布内容；本轮三书源任务快照共 320 条，均为非空正文；无待审核记录。
- [ ] 后续待额度恢复后继续恢复 bqg5555 的 8 个暂停任务；dushuwo 与 bookcheng8 的 16 个任务仍需后台低频执行，完成前不得标记为已完成。

## 2026-09-08 每日额度自动监测与恢复

- [x] 已复核三站正式生效策略：`m.dushuwo.net`、`www.bookcheng8.com`、`www.bqg5555.cc` 均为 `requests_per_minute=2`、`daily_request_limit=10000`、策略启用；每分钟限制与每日限制是两项独立配置。
- [x] 增加 `reader_source_task` 每日重试开关、累计次数、最近日期/时间，以及 `reader_source_task_run` 重试序号、触发类型和触发原因；升级脚本 `script/sql/upgrade/2026-09-08-reader-source-daily-retry.sql` 已执行并验证字段存在。
- [x] 增加独立的每分钟调度器；服务启动不再依赖 Snail Job 开关。调度器按服务器本地日期判断，次日 `00:00` 后检查前一天因“每日请求额度”暂停的任务；每个任务每天最多自动创建一次重试运行记录。
- [x] 管理端 `/reader-admin/source-center` 的“采集任务”增加每日额度自动重试次数和最近时间；“运行记录”增加触发类型/序号、请求数、成功数、失败数、跳过数，任务“配置详情”增加自动重试配置与统计。
- [x] 2026-09-08 23:05:36 首次真实验证：bqg5555 的 8 个每日额度暂停任务全部自动恢复，分别创建 `DAILY_LIMIT`、`retry_no=1` 的运行记录；任务状态均为 `RUNNING`，Worker 已继续按站点低频策略领取任务。
- [ ] 当前全量采集仍未完成：三站合计 24 个分类任务，受每站每分钟 2 次、单 Python Worker 串行执行和长章节请求影响，将继续后台运行；运行记录中的请求/成功/失败统计以实际回执为准，不能提前标记完成。
- [x] 验证：后端全量 Maven 打包通过，管理端 Vite 生产构建通过，书源服务单元测试 `12` 项及阅读器模块全量 `127` 项测试通过，`git diff --check` 通过；后端 PID `61850`，Python Worker PID `54087`。

## 2026-09-08 书源采集中心重复菜单清理

- [x] 根因确认：`sys_menu` 中存在两条相同的 `source-center` 菜单，分别为规范 ID `1761400000000000015` 和重复 ID `1761400000000015000`，管理员角色同时绑定了两条。
- [x] 已执行 `script/sql/upgrade/2026-09-08-reader-source-menu-deduplicate.sql` 两次；删除重复菜单及角色关联，保留规范菜单和管理员角色权限。数据库复核 `source-center` 菜单数量为 `1`。
- [x] 已修正 `2026-09-05-reader-source-center.sql`，后续重复执行按父菜单和路径幂等判断，不再生成重复入口；采集任务、运行记录及采集数据未修改。

## 2026-09-09 异步封面采集真实验收

- [x] 封面脚本已加入 `tools/reader-source-worker/python/cover_crawler.py`，按“书名 + 作者”检索并保存 manifest；公开来源为 Open Library、Google Books，以及已授权书源的公开作品页封面。每条候选保留来源提供方、来源页 URL、原图 URL、方向、尺寸、内容哈希和 OSS ID，便于追溯。
- [x] Java 后端封面任务在章节任务完成后异步创建并执行；目标为每本竖版 3 张、横版 3 张，横版衍生图明确标记为 derived，不冒充来源原图。
- [x] 修复 MinIO 运行环境：数据库配置 `minio / 127.0.0.1:9000 / ruoyi` 与运行凭据 `ruoyi/ruoyi123` 已核对，MinIO API 当前稳定监听 `9000`；保留 OSS 桶私有策略，不整体开放桶。
- [x] 修复私有 OSS 图片展示：新增 `/reader/app/covers/candidates/{candidateId}.png` 站内代理，仅允许读取状态为 `SUCCESS` 且有 OSS 记录的候选；现有候选及作品默认封面已迁移到代理地址，避免使用会过期的预签名 URL。
- [x] 真实重试 13 条封面任务：13/13 `COMPLETED`，每条竖版 `3/3`、横版 `3/3`；共 78 条候选（竖版 39、横版 39），78/78 有来源页、原图地址和 OSS 记录，13/13 作品均已绑定两种方向封面。
- [x] HTTP 核验代理图片返回 `200 image/png`，首张图片 `600x800`，重复请求字节一致；MinIO 连接故障产生的失败候选已清理，成功候选未删除。
- [x] 后端新 JAR 已重新打包并重启，`8080` 已恢复监听；管理端生产构建 `npm run build` 通过，H5 继续运行于 `http://localhost:4173`。

## 2026-09-09 管理端封面候选图显示修复

- [x] 修复管理端作品详情和作品管理“封面设置”中的候选图地址：相对 `/reader/app/covers/...` 路径统一追加管理端当前 `VITE_APP_BASE_API`（开发环境 `/dev-api`、生产环境 `/prod-api`），完整外部地址保持不变。
- [x] 直连后端和管理端网关均已实际验证候选图返回 `200 image/png`，管理端构建通过。

## 2026-09-10 运行租约恢复与真实采集复核

- [x] 定位并修复重复 `LEASE_RECOVERY` 根因：待领取运行与实际被 Worker 领取过的运行此前无法区分，旧租约失效后会反复创建续采记录。
- [x] `reader_source_task_run` 增加 `claimed_at`；只有成功取得 Redis 运行租约的记录才标记为已领取并进入失联回收范围，等待领取的运行不会被误回收。
- [x] Python Worker 心跳改为后端短暂重启期间持续重试，不再因一次连接拒绝直接放弃长章节任务；现有站点限流策略未放宽。
- [x] SQL `script/sql/upgrade/2026-09-10-reader-source-run-claim-state.sql` 已执行，`claimed_at` 字段已验证存在；重复运行历史保留为 `FAILED`，未物理删除。
- [x] Redis 旧运行/站点租约已清理；237 本未完成任务书籍恢复为 `QUEUED`，24 个正式任务各保留一条新的待领取运行记录，服务与 Worker 已重新启动。
- [x] 后端模块编译、后端重新打包、Python Worker 12 项单元测试通过；重启后后端 PID `92181`、Python Worker PID `92185`，监听与认证正常。
- [x] 真实复核确认三站继续按每分钟 2 次、最小间隔 15 秒执行；当前数据库共 17 部作品、1212 个章节、1212 条非空正文。当前采集仍是长时间后台任务，不能提前标记全量完成。
- [x] 新版本运行记录的 `claimed_at` 与 Redis 租约一致；当前仅 3 个跨站运行占用 Worker 槽位，未再出现新的重复运行记录。
- [ ] 24 个分类任务仍需后台继续处理剩余书籍和全部章节；后续按任务明细、快照、作品、章节正文和运行记录持续核验。

## 2026-09-12 采集状态复核与进度修复

- [x] 重新执行后端全量打包并重启 LaunchAgent：后端 PID `90543`，Python Worker PID `90547`；后端 `8080` 响应正常，Worker 使用项目 `.venv` 和最新代码。
- [x] 执行 `script/sql/upgrade/2026-09-12-reader-source-progress-repair.sql`；14 条历史 `COMPLETED/WAITING_REVIEW` 明细补齐实际处理章节数，清理 231 条排队明细的失效 `run_id` 绑定；SQL 可重复执行。
- [x] 修复 Worker 回执：开放章节任务在收到 completed 回执时保存观测到的章节总量；异常重试清除旧书籍运行绑定，避免失效运行导致明细永久卡在 RUNNING。
- [x] 当前三站 24 个任务均无运行中任务：bookcheng8 `FAILED 1`、`PAUSED 7`；dushuwo `PAUSED 8`；bqg5555 `PAUSED 8`。任务书籍明细共 239 条，其中 `COMPLETED 2`、`WAITING_REVIEW 3`、`QUEUED 234`、`RUNNING 0`。
- [x] 已累计处理 5,158 个章节，成功 4,978 个，跳过 180 个；本轮快照 4,978 条且正文非空。已确认终态书籍 5 本：`从军赋` 220/220、`无限恐怖之最强中州队` 140/140（100 成功/40 跳过）、`豪门蜜恋1前夫太欺人` 122/122、`追玫瑰的人` 66/66、`一品绣妃` 70/70。后 3 本处于 `WAITING_REVIEW`，不是任务整体完成。
- [x] 失败原因统计已核验：bqg5555 具体作品请求 HTTP 403（6 次）及每日额度耗尽（3 次）；dushuwo 正文哈希校验失败（6 次）、超时（2 次）和章节号范围异常（2 次）；bookcheng8 旧规则/访问记录包含 approved host 校验失败、SSL 超时、章节范围异常，其中旧 malformed CSS 选择器记录来自历史规则，当前启用规则已为带引号的属性选择器。
- [x] 三站根地址当前真实探测均返回 HTTP `200`；具体作品访问仍受站点 403、超时或内容规则约束，系统按授权策略暂停并记录，不绕过访问控制、不把部分章节标记为整本完成。
- [x] 回归验证：Python Worker 自带测试 `12 tests OK`，Go `go test ./...` 通过，Java `ReaderSourceWorkerServiceTest` 在关闭仓库默认跳过测试配置后 `5 tests OK`；后端重新打包成功。
- [ ] 后续仍需在站点允许访问且额度可用时，通过管理端“恢复”继续 234 本排队书籍；对于 403/SSL/正文校验/目录异常，应先修正书源侧授权或规则后再恢复，不能通过提高频率规避。

## 2026-09-12 采集自动化、失败分类与备用续采完善

- [x] 已执行并两次验证幂等 SQL `script/sql/upgrade/2026-09-12-reader-source-automation.sql`；任务失败分类、普通异常重试、父子任务/备用路由和 `reader_source_task_log` 结构已存在。
- [x] 新增并执行 `script/sql/upgrade/2026-09-12-reader-source-failure-backfill.sql`；历史暂停/失败任务已回填分类：`HTTP_403=6`、`PARSE=5`、`QUALITY=5`、`SSRF=4`、`TIMEOUT=3`、`CIRCUIT_OPEN=2`，日志表补录 25 条迁移识别事件；再次执行未重复插入。
- [x] 可恢复历史异常已安排一次受控自动重试；每日额度、401/403、安全阻断、熔断不会进入普通盲重试。
- [x] 管理端任务列表新增失败分类列，运行记录区分人工启动、自动重试、每日额度、备用续采、分钟限流和租约恢复；任务详情继续提供全链路自动化日志、运行记录和备用书源配置。
- [x] 修复普通自动重试排除条件，`SSRF`、`POLICY`、`CIRCUIT_OPEN` 与授权/额度异常一样不会被自动重试。
- [x] 已重新构建完整 `ruoyi-admin/target/ruoyi-admin.jar` 和管理端；后端 LaunchAgent 当前 PID `53045`，Python Worker 当前 PID `52751`，后端实际加载新代码。
- [x] 重启后真实观察到 13 条 `AUTO_RETRY` 运行记录、8 个任务进入 `RUNNING`，Worker 已真实领取任务并遵守站点最小请求间隔；当前日志表共 51 条。
- [x] 当前三站任务状态：`COMPLETED=5`、`WAITING_REVIEW=3`、`RUNNING=8`、`PAUSED=14`、`FAILED=3`、`CANCELED=1`；仍有 234 本排队/未完成书籍，不能伪造为全量完成。
- [x] 当前暂停原因已可观测：`HTTP_403` 等授权阻断等待站点授权或预配置备用源；`TIMEOUT`、`QUALITY`、`PARSE` 进入有限重试；每日额度任务仅在次日零点后自动检查；所有新处理阶段写入任务日志。
- [ ] 仍未配置任何可用的 `reader_source_task_fallback` 路由，因此 401/403 任务目前只会记录“未配置备用书源”；需管理员为具体任务配置已授权、启用的备用站点/规则/策略后，系统才会自动创建单本续采子任务。
- [x] 修复通用 `HTTP` 故障分类：HTTP 429 归类为 `RATE_LIMIT`，5xx（当前实际为 520）归类为 `HTTP_5XX` 并继续走有界自动重试；管理端已增加“站点 5xx 暂时故障”标签，历史通用 HTTP 记录已迁移。
- [x] 对缺少状态码的 HTTP 回执补充 `HTTP_ERROR` 分类，避免管理端把它显示成无法解释的原始内部代码。
- [x] 补齐批量书籍发现阶段日志：每次 `books` 回执记录发现、接收、跳过数量和是否完成，任务详情日志覆盖目录发现到章节回执的完整链路。
- [x] 最终正式任务汇总（排除 LOCAL_E2E 测试任务）：24 个分类任务当前 `RUNNING=5`、`PAUSED=12`、`FAILED=7`；计划任务书籍明细 239 本，已处理 5 本、成功 5 本、失败明细 84 条；当前没有满足授权条件的备用路由，也没有自动创建跨站子任务。

## 2026-09-13 自动备用书源编排与跨站续采

- [x] 管理端任务详情增加自动扫描备用源、主任务/多级续采子任务、备用站点规则优先级、起始章节、失败分类和进度展示；打开详情会先执行幂等扫描，再加载路由链。
- [x] 后端在任务保存、启动、恢复以及 401/403/5xx 授权阻断时自动扫描其他已授权、启用且配置真实搜索规则的站点；已尝试过的路由不会重复使用，子任务从本地最新章节之后继续。
- [x] Java、Python、Go Worker 均支持备用站点声明式搜索；Java/Go 已补齐 POST 表单搜索，统一使用规则中的 `search.method/url/params`，不猜测或绕过站点访问控制。
- [x] 对现有根任务执行一次幂等数据库回填，`reader_source_task_fallback` 当前共 52 条：dushuwo 与 bqg5555 互为备用，bookcheng8 使用两者作为备用；bookcheng8 未伪造公开搜索入口。
- [x] 验证：后端 Maven 打包成功，管理端 Vite 生产构建成功，Python 语法、Go `go test ./...`、Java Worker Maven 构建和 `git diff --check` 均通过；后端与 Python Worker 已重启并处于运行状态。
- [ ] 管理端当前登录会话因后端重启失效，重新登录后打开 `/reader-admin/source-center` 的任务详情即可看到 52 条路由及主任务/子任务链；未在失效会话下伪造页面验收结果。

## 2026-09-13 403 历史状态诊断与清理

- [x] 根因确认：403 不是站点首页，而是 `https://www.bqg5555.cc/list/{category}/1/` 分类列表请求，8 条记录均发生在 2026-09-12 19:34-22:59；首页可访问不能代表历史分类请求当时可访问。
- [x] 当前真实复测：Worker 的实际请求头访问 bqg5555 首页和分类页均返回 HTTP 200，确认当前不是整站不可访问；403 属于历史时间窗口内的分类路径级间歇性拒绝，原始错误表和任务日志保留追溯证据。
- [x] 修复任务状态污染：启动/恢复、Worker 领取、成功回执和书籍发现成功时显式写入 `failure_code=NULL/fail_reason=NULL`，避免 MyBatis-Plus 忽略 null 导致历史 403 持续显示；自动重试日志不再出现“自动重试中：null”。
- [x] 增加孤儿运行归一化：授权/额度/安全阻断的已结束运行不会继续显示为 `RUNNING`，只恢复为明确的 `PAUSED`，不绕过 403 也不盲目重试。
- [x] 定向修复当前 3 条旧运行残留；当前运行任务不再显示旧 `HTTP_403`，历史 `reader_source_error` 中 8 条 403 保留未解决状态供审核。
- [x] 后端已重新打包并重启，当前 PID `34619`；Python Worker 当前 PID `21092`；后端首页返回 200，bqg5555 分类页 Worker 请求返回 200。
- [x] 回归验证：Python `py_compile`、Go `go test ./...`、`git diff --check` 通过。

## 2026-09-13 熔断任务人工确认重试

- [x] 管理端在 `CIRCUIT_OPEN` 且任务状态为 `FAILED/PAUSED` 时显示“重置熔断重试”，任务配置详情同步提供相同操作；确认弹窗明确说明会清理连续失败计数和熔断标志，原错误记录与日志保留。
- [x] 后端新增 `POST /reader/admin/source/tasks/{taskId}/retry-circuit`：校验失败分类和任务状态，自动补充备用书源，清理 Redis 站点失败计数/熔断标志，创建 `CIRCUIT_RETRY` 运行记录并重新进入 `RUNNING`；不会加入普通自动盲重试。
- [x] 前端 API 引用、后端测试装配和熔断重试专项测试已补齐；`ReaderSourceServiceTest` 13 项测试通过，管理端 `npm run build` 通过，`git diff --check` 通过。
- [ ] 后端重启后需在管理端登录状态下对实际 `CIRCUIT_OPEN` 任务点击确认重试，并观察最新运行记录是否被 Worker 领取；历史错误和任务日志应继续可查。

## 2026-09-13 采集中心全链路大盘

- [x] 新增后端大盘接口：总览统计、小说聚合列表、小说任务链详情、小说章节正文状态分页；统计实时聚合任务、运行、任务书籍、作品、章节、正文、快照、错误、日志、站点、备用书源、发现源和封面任务，不新增冗余统计表。
- [x] 新增管理端“采集数据大盘”页面：核心指标、任务/运行/书籍状态、章节计划与处理、错误分类、运行趋势、站点对比、中心资源清单、小说聚合追踪和小说详情下钻。
- [x] 小说下钻支持查看当前任务、多级任务链、每本小说的章节总数/正文可用/正文缺失/发布状态、最近错误和自动化日志；章节正文只查询正文存在性，不加载正文大字段。
- [x] 新增菜单升级脚本 `script/sql/upgrade/2026-09-13-reader-source-dashboard.sql`；修正与既有作品分类菜单的 ID 冲突后已执行两次并验证幂等，`source-dashboard` 菜单 1 条、管理员角色授权 1 条。
- [x] 后端 `mvn -pl ruoyi-admin -am -DskipTests package` 成功，管理端 `pnpm build` 成功；后端已重启并稳定监听 `8080`，未登录接口按权限返回 401。
- [ ] 浏览器页面点击验收待当前 Mac 解锁并恢复管理端登录会话后进行；菜单入口为“阅读器管理 -> 采集数据大盘”，地址 `/reader-admin/source-dashboard`。

## 2026-09-13 租约失效导致任务假运行修复

- [x] 根因确认：Worker 运行租约或站点并发租约丢失后，旧心跳只返回错误，没有结束数据库运行记录；旧 Worker 随后退出，造成运行记录、任务书籍长期保持 `RUNNING`。
- [x] 修复心跳处理：心跳先校验运行令牌和数据库状态，再续租运行与站点槽位；租约失效时立即结束旧运行、释放书籍、释放租约并创建 `LEASE_RECOVERY` 续采运行，迟到回执由运行状态校验拒绝。
- [x] 修复 stale recovery：已成功领取但 Redis 租约已丢失的运行不再被误判为“等待领取”；只有从未领取的排队运行保留等待状态。
- [x] 增加 `ReaderSourceWorkerServiceTest` 两项续租/租约丢失回归测试；Worker 服务测试和后端打包通过，`git diff --check` 通过。
- [x] 后端已重启，当前 PID `94849`；Python Worker 已重启，当前 PID `95156`。重启后任务书籍 `RUNNING` 明细由 12 降至 5，旧失效运行已开始归档并重新排队。
- [x] 当前真实状态：正式 24 个分类任务中 `RUNNING=15`、`PAUSED=2`、`FAILED=1`、`COMPLETED=0`（任务生命周期状态）；任务书籍为 `QUEUED=219`、`RUNNING=5`、`COMPLETED=11`、`WAITING_REVIEW=6`。任务仍未全量完成。
- [x] 当前站点策略仍为每站并发 `1`、每分钟 `2` 次、最小间隔 `15` 秒、每日 `10000` 次；当前按策略低频执行，不能通过提高频率绕过来源站点限制。
- [ ] 继续观察新 `LEASE_RECOVERY` 运行的章节回执和任务进度；若出现新的 `PARSE`、`HTTP_403`、`TIMEOUT` 或 `HTTP_5XX`，按任务详情中的日志和失败分类分别处理。

## 2026-09-13 最终运行核验

- [x] 后端 HTTP 返回 `200`，LaunchAgent 后端与 Python Worker 均处于运行状态；Redis 当前最多每个站点一个并发租约，未再发现同站点重复占用。
- [x] 旧 Worker 租约修复后，正式任务书籍明细当前为 `QUEUED=230`、`RUNNING=1`、`COMPLETED=11`、`WAITING_REVIEW=6`；同一批量运行下重复 `RUNNING` 书籍已清理。
- [x] 24 个正式分类任务当前：bookcheng8 `FAILED=2/PAUSED=6`，dushuwo `FAILED=1/PAUSED=7`，bqg5555 `RUNNING=7/PAUSED=1`。其中 bookcheng8 与 dushuwo 的暂停主要是站点熔断后的人工确认等待，不是 Worker 卡死。
- [x] 失败分类累计仍以 `PARSE` 和站点 `HTTP 403/520` 为主；dushuwo 当前 Redis 熔断标记存在，继续点击恢复前应先确认站点访问和规则质量，不能用提高请求频率规避。
- [x] 全库已有 7,136 条非空采集快照、7,006 条小说章节；本轮新增章节回执需继续在来源站点解除熔断并恢复运行后增长，当前未达到三站全量完成。
